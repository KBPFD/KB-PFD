# Optional: bake your Google OAuth client ID into the build so you don't have to
# paste it on the login screen. Copy this file to `.env` and fill in the value.
# This ID is public by design (it is visible in any browser app) - it is NOT a secret.
VITE_GOOGLE_CLIENT_ID=
